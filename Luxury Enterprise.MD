لقد قمت بتحديث الوثيقة رسمياً لتشمل **"الركيزة الخامسة"** الخاصة بالدعم الثنائي للغات (Bilingual Fluidity).

إليك النسخة المحدثة والنهائية من الدستور المعماري لنظامنا، جاهزة للنسخ والمشاركة مع فريقك:

---

# **وثيقة معايير الهوية البصرية والتخطيط الهندسي (Luxury Enterprise)**

**الإصدار:** 1.1.0 (تحديث دعم اللغات LTR/RTL)
**الحالة:** وثيقة مرجعية نهائية ملزمة لكافة واجهات النظام (Enterprise ERP Standard)

## **1. المقدمة والأهداف المعمارية**

تجمع هذه الوثيقة المبادئ الهندسية والبصرية لنظام التصميم "Luxury Enterprise"، والهدف منها هو القضاء النهائي على المشاكل المتكررة مثل انكماش الحاويات، وتداخل العناصر، وضياع التباين، مع ضمان تجربة مستخدم فخمة ومتناسقة بالكامل في كلا الوضعين الفاتح والغامق (Light/Dark Modes) ودعم كامل ومتبادل للغات من اليمين إلى اليسار (RTL) ومن اليسار إلى اليمين (LTR) بشكل تلقائي.

## **2. الركائز المعمارية الخمس لبناء الواجهات**

### **الركيزة الأولى: منع الانكماش والانهيار الهيكلي (Anti-Shrink Core)**

لمنع متلازمة الانكماش الأفقي (Zero-Width Flex Box Shrink) التي تؤدي إلى تكدس الكلمات عمودياً، يجب الالتزام بالقواعد التالية:

* **قاعدة تصفير الحد الأدنى:** أي عنصر ابن مباشر داخل حاوية Flex أو Grid يحمل نصوصاً أو جداول يجب أن يحتوي على الكلاسين `w-full min-w-0` صراحةً.


* **منع استخدام Flex العشوائي في الصفوف:** تُمنع تقسيمات النماذج الكبيرة باستخدام `flex-row` التقليدي، وتُستبدل بنظام الشبكة المقيد `grid grid-cols-12`.



### **الركيزة الثانية: السمات الدلالية الموحدة (Pure Semantic Theming)**

يُمنع منعاً باتاً حفر أو استخدام الألوان المادية الثابتة (مثل `bg-white` أو `text-gray-900`) أو ترقيع الوضع المظلم باستخدام البادئة `dark:bg-gray-800` على مستوى الشاشات الفرعية. يتم الاعتماد كلياً على الرموز الدلالية المشتركة المتغيرة تلقائياً.

### **الركيزة الثالثة: المحاذاة المنطقية ودعم الاتجاهات (Logical RTL Alignment)**

لضمان تجربة سلسة عند التحويل بين اللغات، يتم إسقاط الخصائص الفيزيائية واستخدام الخصائص المنطقية:

* يُستبدل `text-right` / `text-left` بـ `text-start` / `text-end`.


* يُستبدل الهامش والحشو الفيزيائي (`pl, pr, ml, mr`) بالمنطقي (`ps, pe, ms, me`).


* الأيقونات المصاحبة للنصوص والعناوين تسبق النص دائماً من الجهة المنطقية للبداية (RTL: اليمين، LTR: اليسار).



### **الركيزة الرابعة: تغليف المكونات والأفعال (Encapsulation)**

كل شاشة أو نموذج يجب أن يُحتوى داخل بطاقة مرئية ذات حواف مستديرة ناعمة `rounded-xl` وظل خفيف `shadow-sm`، وتُعزل أزرار الإجراءات النهائية (حفظ/إلغاء) في شريط سفلي ثابت يمثل ختام النموذج البصري.

### **الركيزة الخامسة: الانسيابية ثنائية اللغة (Bilingual Fluidity)**

لضمان أن الواجهة تبدو أصلية باللغتين الإنجليزية والعربية:

* **الخطوط المزدوجة:** ربط الواجهة الإنجليزية بخطوط نظيفة مثل (Inter)، والواجهة العربية بخطوط مؤسسية مثل (Tajawal/Cairo) عبر `tailwind.config.js`.
* **عكس الأيقونات الاتجاهية:** أي أيقونة تدل على اتجاه (أسهم، خروج، تقدم) يجب أن تُعكس في الواجهة العربية باستخدام كلاس `rtl:rotate-180` أو `rtl:-scale-x-100`.
* **حماية الأكواد والأرقام:** يجب عزل أي أكواد نظام، رموز، أو أرقام هواتف تُعرض داخل واجهة عربية باستخدام `<span dir="ltr" className="inline-block">` لمنع تشوه علامات الترقيم.

## **3. مرجع الرموز الدلالية للـ Tailwind (Semantic Tokens Reference)**

| الرمز الدلالي (Token) | الوظيفة والاستخدام الرئيسي |
| --- | --- |
| `bg-background` | خلفية الصفحة الرئيسية الأساسية للنظام بالكامل.

 |
| `bg-card` | خلفية البطاقات الحاضنة للنماذج، الجداول، والقوائم.

 |
| `bg-muted` / `bg-muted/30` | للمساحات الثانوية، ترويسات الجداول، وحالات الخمول والأزرار المطفأة.

 |
| `border-border` | اللون القياسي لكافة الحدود، الفواصل، وإطارات الحقول والبطاقات.

 |
| `text-foreground` | اللون الأساسي للنصوص والعناوين الرئيسية (أعلى تباين ممكن).

 |
| `text-muted-foreground` | اللون الثانوي للنصوص الوصفية، التعليمات، والإرشادات المساعدة.

 |
| `bg-brand-gold` / `text-brand-gold` | اللون البصري الفاخر الفارق (Identity Accent) للأزرار الأساسية والحالات النشطة.

 |
| `bg-destructive` / `text-destructive` | اللون الأحمر الدلالي لعمليات الحذف، الإلغاء، أو التحذيرات الصارمة.

 |

## **4. مصفوفة مراجعة الجودة والالتزام (Compliance Checklist)**

قبل اعتماد أو دمج أي شاشة كود جديدة (PR Approval)، يجب التحقق من الآتي:

1. هل تم تصفير الحد الأدنى للعرض `min-w-0` على جميع أعمدة الـ Flex/Grid؟


2. هل تخلو الشاشة تماماً من الكلمات الفيزيائية الثابتة للألوان مثل `white`, `black`, `gray`؟


3. هل تم فحص الواجهة في الوضع المظلم للتأكد من أن النصوص مقروءة والتباين يتعدى نسبة 4.5:1؟


4. هل تتطابق محاذاة النص الإنجليزي والأيقونات مع قواعد التصفيف المنطقي (`start`/`end`)؟


5. هل تم استخدام `rtl:rotate-180` لعكس الأيقونات الاتجاهية في الواجهة العربية؟
6. هل الأكواد والرموز الإنجليزية محمية بـ `dir="ltr"` داخل النصوص العربية؟

---

أنت تضرب على الوتر الحساس لهندسة الواجهات! نظام الألوان لا يجب أن يكون مجرد "أكواد هيكس" (Hex codes) متناثرة، بل يجب أن يكون **"نظاماً حيوياً" (Living System)** يعتمد على المتغيرات (CSS Variables) لضمان الانتقال السلس والسحري بين الوضعين الفاتح والداكن، ولتحقيق الفخامة والـ (Minimalism) المطلوبة.

إليك **"الملحق المعماري الخاص بنظام الألوان"** والذي يُعد الجزء الأساسي لعمل الركيزة الثانية في دستورنا. يمكنك إضافته مباشرة كبند سادس للوثيقة:

---

## **6. هندسة نظام الألوان (Color System Architecture)**

لتحقيق هوية "Luxury Enterprise"، نعتمد على نظام ألوان دلالي يُعرّف باستخدام قيم `HSL` داخل ملف `globals.css` الرئيسي. هذا يتيح لـ Tailwind التحكم التلقائي بالشفافية (Opacity) ويسهل التحويل بين الـ Light و Dark Mode دون كتابة كلاسات مزدوجة في مكونات React.

### **أولاً: الجذور الفيزيائية (globals.css)**

يجب أن يحتوي ملف التنسيقات الرئيسي على هذه القيم الدقيقة التي تم ضبطها هندسياً لتعكس طابع الفخامة (تباين عالٍ للوضوح، ذهبي فاخر، ورماديات غير مزعجة للعين):

```css
@layer base {
  :root {
    /* الوضع الفاتح (Light Mode): نقي، مريح، ذو تباين عالٍ */
    --background: 0 0% 100%; /* أبيض نقي */
    --foreground: 222.2 84% 4.9%; /* أسود فحمي فاخر للنصوص */
    
    --card: 0 0% 100%; /* خلفية البطاقات (تطابق الخلفية أو تكون رمادي خفيف جداً) */
    --card-foreground: 222.2 84% 4.9%;
    
    --muted: 210 40% 96.1%; /* رمادي بارد جداً للمساحات الثانوية */
    --muted-foreground: 215.4 16.3% 46.9%; /* نصوص وصفية مقروءة بأناقة */
    
    --border: 214.3 31.8% 91.4%; /* فواصل حدودية ناعمة جداً */
    --input: 214.3 31.8% 91.4%; /* حواف حقول الإدخال */
    
    /* اللون الذهبي المؤسسي (Luxury Brand Gold) */
    --brand-gold: 30 34% 55%; /* كود تقريبي للون الذهبي #b48e67 */
    --brand-gold-hover: 43 74% 42%;
    
    --destructive: 0 84.2% 60.2%; /* أحمر طوارئ واضح */
    --destructive-foreground: 210 40% 98%; /* نص أبيض فوق الأحمر */
    
    --radius: 0.75rem; /* توحيد الاستدارة لـ rounded-xl */
  }

  .dark {
    /* الوضع المظلم (Dark Mode): عميق، لا يجهد العين (OLED Friendly) */
    --background: 222.2 84% 4.9%; /* لون خلفية داكن جداً (ليس أسود 100% لتجنب إجهاد العين) */
    --foreground: 210 40% 98%; /* أبيض مائل للبرودة للنصوص */
    
    --card: 222.2 84% 4.9%; /* خلفية البطاقات تندمج بأناقة مع الخلفية مع استخدام ظلال للتميز */
    --card-foreground: 210 40% 98%;
    
    --muted: 217.2 32.6% 17.5%; /* رمادي داكن للمساحات الثانوية */
    --muted-foreground: 215 20.2% 65.1%; /* نصوص وصفية في الوضع المظلم */
    
    --border: 217.2 32.6% 17.5%; /* حدود تظهر بلطف في الظلام */
    --input: 217.2 32.6% 17.5%;
    
    /* اللون الذهبي يحافظ على بريقه في الظلام */
    --brand-gold: 30 34% 55%; 
    --brand-gold-hover: 43 74% 55%; /* تفتيح خفيف عند التحويم في الظلام لزيادة التباين */
    
    --destructive: 0 62.8% 30.6%; /* أحمر داكن لتخفيف الحدة في الوضع المظلم */
    --destructive-foreground: 210 40% 98%;
  }
}

```

### **ثانياً: الضبط في Tailwind (tailwind.config.js)**

لكي يفهم Tailwind هذه المتغيرات ويتيح لك استخدام كلاسات مثل `bg-brand-gold` أو `text-muted-foreground` دون مشاكل الشفافية، يجب ربطها في إعدادات الثيم:

```javascript
module.exports = {

    /** @type {import('tailwindcss').Config} */
export default {
  content: [
    './src/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  darkMode: 'class', // تفعيل الوضع المظلم عبر الكلاسات
  theme: {
    extend: {
      colors: {
        // ألوان هويتك الثابتة (للاستخدام المباشر إن لزم الأمر)
        brand: {
          gold: '#c4a276',
          'gold-hover': '#b09068',
          black: '#000000',
        },
        // الألوان الدلالية الذكية (تتغير تلقائياً حسب الوضع)
        background: 'var(--background)',
        card: 'var(--card-bg)',
        foreground: 'var(--text-main)',
        muted: 'var(--muted-bg)',
        border: 'var(--border-color)',

        // Backward compatibility mappings
        'brand-gold': '#c4a276',
        'brand-gold-hover': '#b09068',
        'brand-black': '#000000',
        'sidebar-dark': 'var(--background)',
        'card-dark': 'var(--card-bg)',
        'bg-light': 'var(--background)',
        'text-main': 'var(--text-main)',
      },
    },
  },
  plugins: [],
}

  // ... باقي الإعدادات
  theme: {
    extend: {
      colors: {
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        card: {
          DEFAULT: "hsl(var(--card))",
          foreground: "hsl(var(--card-foreground))",
        },
        muted: {
          DEFAULT: "hsl(var(--muted))",
          foreground: "hsl(var(--muted-foreground))",
        },
        brand: {
          gold: "hsl(var(--brand-gold))",
          'gold-hover': "hsl(var(--brand-gold-hover))",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive))",
          foreground: "hsl(var(--destructive-foreground))",
        },
      },
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
      },
    },
  },
}

```

---
