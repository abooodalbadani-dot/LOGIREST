## TOP 20 FILES WITH LOST BUSINESS LOGIC / EXCLUSIVE WORK IN BACKUP

### `[locale]\(auth)\reset-password\page.tsx` (85 potential lost lines)
**Verdict**: MINOR TEXT/STYLE DIFFS
**Sample of lost code/styles:**
```typescript
- import { Link, useRouter } from '@/i18n/navigation';
- import { useParams, useSearchParams } from 'next/navigation';
- UtensilsCrossed,
- import { Button } from '@/components/ui/button';
- import { Input } from '@/components/ui/input';
```
----------------------------------------
### `[locale]\(app)\(operations)\adjustments\new\AdjustmentCreateClient.tsx` (75 potential lost lines)
**Verdict**: BACKUP HAS VISUAL IMPROVEMENTS (Premium design lost in active)
**Sample of lost code/styles:**
```typescript
- const AdjustmentFormSchema = z.object({
- warehouse_id: z.string().min(1, 'Warehouse is required'),
- item_id: z.string().min(1, 'Item is required'),
- quantity: z.number().positive('Quantity must be greater than zero'),
- reason_category: z.string().min(1, 'Reason category is required'),
```
----------------------------------------
### `[locale]\(auth)\login\page.tsx` (73 potential lost lines)
**Verdict**: MINOR TEXT/STYLE DIFFS
**Sample of lost code/styles:**
```typescript
- import { Loader2, AlertCircle, UtensilsCrossed, ShieldCheck, BadgeCheck, Key, EyeOff, LogIn } from 'lucide-react';
- console.log(`[LoginPage] hero_title: ${t('login.hero_title')}`);
- <div className="flex w-full items-center justify-center min-h-screen bg-background">
- <Loader2 className="w-8 h-8 animate-spin text-operational-cyan" />
- <div className="flex w-full min-h-screen lg:h-screen lg:overflow-hidden bg-background">
```
----------------------------------------
### `[locale]\(auth)\forgot-password\page.tsx` (69 potential lost lines)
**Verdict**: MINOR TEXT/STYLE DIFFS
**Sample of lost code/styles:**
```typescript
- import { Link, useRouter } from '@/i18n/navigation';
- UtensilsCrossed,
- import { Button } from '@/components/ui/button';
- import { Input } from '@/components/ui/input';
- } from '@/components/ui/form';
```
----------------------------------------
### `[locale]\(app)\(operations)\kitchen-requests\new\KitchenRequestFormClient.tsx` (33 potential lost lines)
**Verdict**: BACKUP HAS EXCLUSIVE FEATURES: ['camelCase_properties']
**Sample of lost code/styles:**
```typescript
- items: [{ itemId: '', quantity: 1, notes: '' }],
- createRequest.mutate({ ...values, isDraft }, {
- <div className="p-8 max-w-[1200px] mx-auto space-y-8 animate-in fade-in slide-in-from-bottom-4 duration-1000">
- className="rounded-xl h-11 px-6 text-label-xs font-semibold uppercase transition-all"
- "bg-cyan-600 hover:bg-cyan-500 text-white rounded-xl h-11 px-8 text-label-xs font-semibold uppercase transition-all shadow-lg"
```
----------------------------------------
### `[locale]\(app)\(operations)\issues\new\issue-form.tsx` (29 potential lost lines)
**Verdict**: MINOR TEXT/STYLE DIFFS
**Sample of lost code/styles:**
```typescript
- itemId: z.string().min(1, t('validation.item_required')),
- requestedQuantity: z.number().min(0.01, t('validation.qty_positive')),
- allocatedQuantity: z.number(),
- lots: z.array(z.custom<IssueLot>()),
- warehouseId: z.string().min(1, t('validation.warehouse_required')),
```
----------------------------------------
### `[locale]\(app)\(operations)\adjustments\[id]\AdjustmentForm.tsx` (24 potential lost lines)
**Verdict**: ACTIVE CODE IS FUNCTIONALLY SUPERIOR (Has Lock States & Idempotency)
**Sample of lost code/styles:**
```typescript
- abortController.signal
- await createAdjustment.mutateAsync({ payload, signal: abortController.signal });
- await updateAdjustment.mutateAsync({ id, payload, signal: abortController.signal });
- if (!!lockState?.isLocked || !canEdit) return;
- const res = await apiClient.get(`/master-data/items?barcode=${barcode}`, ItemSchema, abortController.signal);
```
----------------------------------------
### `[locale]\(app)\(operations)\kitchen-requests\[id]\KitchenRequestForm.tsx` (24 potential lost lines)
**Verdict**: ACTIVE CODE IS FUNCTIONALLY SUPERIOR (Has Lock States & Idempotency)
**Sample of lost code/styles:**
```typescript
- const [fulfillmentData, setFulfillmentData] = useState<{ itemId: string; fulfilledQuantity: number }[]>([]);
- await updateStatus.mutateAsync({ id, status: KITCHEN_REQUEST_STATUS.APPROVED, version: request.version ?? 0 });
- await updateStatus.mutateAsync({ id, status: KITCHEN_REQUEST_STATUS.CANCELLED, reason: trimmedReason, version: request.version ?? 0 });
- await fulfillRequest.mutateAsync({ id, items: fulfillmentData, version: request.version ?? 0 });
- itemId: item.item_id,
```
----------------------------------------
### `[locale]\(app)\(operations)\yield-management\YieldManagementClient.tsx` (18 potential lost lines)
**Verdict**: MINOR TEXT/STYLE DIFFS
**Sample of lost code/styles:**
```typescript
- recipe_name: 'Sous-vide Ribeye Prep',
- category: 'Protein',
- recipe_name: 'Fresh Pasta Dough',
- category: 'Starch',
- recipe_name: 'Trimmed Asparagus',
```
----------------------------------------
### `[locale]\(app)\(operations)\stocktake\[id]\count\StocktakeCountClient.tsx` (14 potential lost lines)
**Verdict**: ACTIVE CODE IS LARGER (Active contains more complete/fixed logic)
**Sample of lost code/styles:**
```typescript
- import { Badge } from "@/components/ui/badge";
- import { PostConfirmDialog } from "@/components/shared/PostConfirmDialog";
- import { type DocumentStatus } from "@/core/workflow/document-engine";
- const { user } = useAuth();
- updateCount.mutate({ stocktakeId: id, itemId, lineId, countedQty, signal: abortController.signal })
```
----------------------------------------
### `[locale]\(app)\(operations)\transfers\[id]\ship\TransferShipClient.tsx` (14 potential lost lines)
**Verdict**: ACTIVE CODE IS FUNCTIONALLY SUPERIOR (Has Lock States & Idempotency)
**Sample of lost code/styles:**
```typescript
- import { useState, useCallback } from 'react';
- const { data: transfer, isLoading } = useTransfer(id);
- const shipTransfer = useShipTransfer();
- }, [transfer, t, locale]);
- { id, version: transfer.version || 1 },
```
----------------------------------------
### `[locale]\(app)\(operations)\issues\IssueListClient.tsx` (13 potential lost lines)
**Verdict**: ACTIVE CODE HAS VISUAL IMPROVEMENTS (Premium styles/animations)
**Sample of lost code/styles:**
```typescript
- <div className="flex flex-col">
- <span className="text-label-xxs font-semibold text-muted-foreground/30 uppercase">
- {row.original.destination_department_id || 'ù'}
- <div className="flex flex-col items-start gap-0.5">
- className="text-label-xxs font-semibold text-muted-foreground/20 uppercase"
```
----------------------------------------
### `[locale]\(app)\(operations)\transfers\[id]\receive\TransferReceiveClient.tsx` (9 potential lost lines)
**Verdict**: ACTIVE CODE IS FUNCTIONALLY SUPERIOR (Has Lock States & Idempotency)
**Sample of lost code/styles:**
```typescript
- import { useState, useMemo, useEffect } from 'react';
- import { canPerformActionV2, type DocumentStatus } from '@/core/workflow/document-engine';
- <DocumentLockWrapper isLocked={false}>
- disabled={!hasVariance}
- : 'border-white/5 opacity-40 cursor-not-allowed'
```
----------------------------------------
### `[locale]\(app)\(operations)\transfers\new\TransferNewClient.tsx` (9 potential lost lines)
**Verdict**: ACTIVE CODE IS FUNCTIONALLY SUPERIOR (Has Lock States & Idempotency)
**Sample of lost code/styles:**
```typescript
- import { useState } from 'react';
- const item = items?.find(i => i.sku === barcode);
- if (!item) return;
- primary_uom: { code: item.uom }
- uom_id: item.uom
```
----------------------------------------
### `[locale]\(app)\communications\notifications\templates\[id]\TemplateEditorClient.tsx` (8 potential lost lines)
**Verdict**: MINOR TEXT/STYLE DIFFS
**Sample of lost code/styles:**
```typescript
- {updateMutation.isPending ? '...' : t('subject_ar').includes('┌╤╚و') ? '═▌┘ ╟ط╩█وو╤╟╩' : 'Save Changes'}
- {t('retry') === '┼┌╟╧╔ ╟طع═╟µط╔' ? '┼ط█╟┴' : 'Cancel'}
- {t('code')}: <span className="font-mono text-foreground">{data?.code}</span>
- <span className="text-label-xs font-semibold uppercase text-foreground/80">{t('preview')}</span>
- <span className="px-2 py-0.5 rounded bg-operational-cyan/10 text-operational-cyan text-label-xxs font-semibold uppercase border border-operational-cyan/20">Live View</span>
```
----------------------------------------
### `[locale]\(app)\(operations)\issues\[id]\scan-mode\page.tsx` (7 potential lost lines)
**Verdict**: MINOR TEXT/STYLE DIFFS
**Sample of lost code/styles:**
```typescript
- if (isLoading) return <div className="p-8 text-center">Loading...</div>;
- <span className="text-operational-cyan/80 text-label-sm font-mono uppercase italic">Immersive Scan Mode</span>
- <Button variant="outline" size="sm">Exit Scan Mode</Button>
- placeholder="READY FOR BARCODE..."
- <div className="text-label-sm text-muted-foreground uppercase mb-1">Quantity</div>
```
----------------------------------------
### `[locale]\(app)\master-data\items\ItemFormClient.tsx` (7 potential lost lines)
**Verdict**: MINOR TEXT/STYLE DIFFS
**Sample of lost code/styles:**
```typescript
- } catch (_error) {
- {errors.code && <p className="text-label-xs font-semibold text-status-error uppercase">{tv(errors.code.message as any)}</p>}
- {errors.barcode && <p className="text-label-xs font-semibold text-status-error uppercase">{tv(errors.barcode.message as any)}</p>}
- {errors.name_en && <p className="text-label-xs font-semibold text-status-error uppercase">{tv(errors.name_en.message as any)}</p>}
- {errors.name_ar && <p className="text-label-xs font-semibold text-status-error uppercase">{tv(errors.name_ar.message as any)}</p>}
```
----------------------------------------
### `[locale]\(app)\(operations)\transfers\TransferListClient.tsx` (6 potential lost lines)
**Verdict**: ACTIVE CODE HAS VISUAL IMPROVEMENTS (Premium styles/animations)
**Sample of lost code/styles:**
```typescript
- <div className="text-label-xxs font-semibold text-muted-foreground/40" dir="ltr">
- <Button className="h-11 px-8 bg-cyan-600 hover:bg-cyan-500 text-white text-label-xs font-semibold uppercase rounded-md transition-all shadow-lg shadow-cyan-900/10">
- <div className="flex flex-wrap items-end gap-6 w-full py-6 px-8 bg-surface-container-low border border-outline-low/5 rounded-lg shadow-sm">
- <SelectTrigger className="w-full bg-surface-container-highest/40 border-none h-12 px-4 text-label-sm font-semibold rounded-md transition-all hover:bg-surface-container-highest/60 focus:ring-1 focus:ring-cyan-500/10">
- className="w-full bg-surface-container-highest/40 border-none h-12 ps-12 pe-4 text-label-sm font-semibold rounded-md transition-all group-hover:bg-surface-container-highest/60 focus:ring-1 focus:ring-cyan-500/10"
```
----------------------------------------
### `[locale]\not-found.tsx` (6 potential lost lines)
**Verdict**: MINOR TEXT/STYLE DIFFS
**Sample of lost code/styles:**
```typescript
- <h3 className="text-label-xs font-bold uppercase tracking-[0.3em] text-muted-foreground/40">Our Mission</h3>
- &quot;At LogiRest, we believe in frictionless precision. Our system is built to ensure that not a single gram of inventory is lost to the void of inefficiency.&quot;
- <h3 className="text-label-xs font-bold uppercase tracking-[0.3em] text-muted-foreground/40">Trusted By Industry Leaders</h3>
- <span className="text-headline-xs font-black tracking-tighter">GLOBAL_KITCHEN</span>
- <span className="text-headline-xs font-black tracking-tighter">RESTO_PRO</span>
```
----------------------------------------
### `[locale]\(app)\master-data\warehouses\WarehouseListClient.tsx` (5 potential lost lines)
**Verdict**: MINOR TEXT/STYLE DIFFS
**Sample of lost code/styles:**
```typescript
- MAIN: { label: t('types.MAIN'), color: 'text-primary', shadow: 'shadow-[0_0_8px_rgba(var(--primary-rgb),0.4)]' },
- COLD: { label: t('types.COLD'), color: 'text-operational-cyan', shadow: 'shadow-[0_0_8px_rgba(var(--cyan-rgb),0.4)]' },
- DRY: { label: t('types.DRY'), color: 'text-operational-orange', shadow: 'shadow-[0_0_8px_rgba(var(--orange-rgb),0.4)]' },
- physical: warehouses.filter(w => w.type === 'MAIN' || w.type === 'COLD').length,
- ], [tc, t, locale, router, WAREHOUSE_TYPE_STYLES]);
```
----------------------------------------
