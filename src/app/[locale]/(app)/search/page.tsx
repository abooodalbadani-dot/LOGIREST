import SearchClient from './SearchClient';

export const metadata = {
 title: 'Global Search | LogiRest',
 description: 'Unified search for LogiRest Inventory System',
};

export default function SearchPage() {
 return <SearchClient />;
}
